このファイルにはこのライブラリ全体を通した更新状況をメモ程度に残します。

2012/5/4	各クラスの名前空間の変更を実施
2012/5/12	MapBaseクラスをMapクラスと名前を変更した。
			また、同時にMapBase名前空間を新設
2012/5/14	メモ：
			MapField中のFixFieldメソッドはMapクラスへ移す
			MapCorner構造体をGNSS名前空間内のRectangleFieldクラスへ置換したい。
			置換するには、この構造体を利用して値の代入を行っている構文が邪魔になる。
2012/5/18	MapCornerクラスが機能をGNSS名前空間にあるRectangleFieldクラスが全てカバーしているのでこれを廃止する。
			以上に伴い、各ファイルの修正を図る。
2012/5/24	DEM管理クラス群を新規開発中。
			また、それに伴いKML名前空間の新設とPolygonクラスを追加した。
2012/5/28	DEMに関するクラス再編をほぼ終了した。
			マップ読み込みの高速化も図った。
			現時点での成績は、5 mメッシュデータ685ファイル読み込みに2.7～3.5 s(@ Core i7 2600K 3.4GHz, RAM 16 GByte)。
			ヘッダ情報のみなら、0.16 s/ 1293 file よりも早い。
			マップの結合も実用速度にはあるので何かない限り手を入れないと思う。
			今後の拡張の方向としては、GNSSに特化した開空度計算クラスの新設だろうと思う。
2012/5/29   MapクラスのSaveAsImage()を高速化
2012/6/6	KMLクラス群にPointクラスを追加。独自に作っていたColor構造体を廃止。
2012/7/12	バインドされているオブジェクト同士の結合でも支障がないようにした。
			また、GetKmlCode()メソッドの呼び出しでstyleIDが書き換わってしまうのを避ける構造にした。